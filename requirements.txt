# Anki Q&A Extractor Dependencies
#
# Install in a conda environment:
#   conda create -n anki-extractor python=3.12 -y
#   conda activate anki-extractor
#   pip install -r requirements.txt

# PDF Processing
docling>=2.0.0              # PDF to Markdown conversion
pymupdf>=1.24.0             # Image extraction from PDFs (import as 'fitz')

# Anthropic API (for agentic LLM calls)
anthropic>=0.40.0           # Claude API client
python-dotenv>=1.0.0        # Load environment variables from .env file

# Anki Deck Generation
genanki>=0.13.0             # Create .apkg Anki deck files

# GUI for Review
streamlit>=1.30.0           # Web-based GUI for image-question review
pillow>=10.0.0              # Image processing for Streamlit

# Export
openpyxl>=3.1.0             # Excel export for stats

# Utilities (usually pre-installed, but listed for completeness)
# These are standard library or common dependencies
# json, re, base64, os, sys, pathlib - all standard library
